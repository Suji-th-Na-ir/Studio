using UnityEngine;
using Leopotam.EcsLite;

namespace Terra.Studio
{
    public class REPLACEMESystem : IAbsRunsystem, IConditionalOp
    {
        public void Init(EcsWorld currentWorld)
        {
            var filter = currentWorld.Filter<REPLACEMEComponent>().End();
            var pool = currentWorld.GetPool<REPLACEMEComponent>();
            foreach (var entity in filter)
            {
                
            }
        }

        public void OnConditionalCheck(object data)
        {
            
        }

        public void OnDemandRun(EcsWorld currentWorld)
        {

        }
    }
}
